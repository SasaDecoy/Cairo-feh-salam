% =====================================================================
%  Cairo Public Transport · Comprehensive Formal Report — preamble
%  Dark Plotly palette, terminal-style code blocks, callout boxes.
%  Compile with:  pdflatex -shell-escape main.tex   (run twice for ToC)
% =====================================================================

% ── Packages ──────────────────────────────────────────────────────────
%  Compile with xelatex so Arabic characters render natively.
\usepackage{fontspec}
\usepackage{polyglossia}
\setdefaultlanguage{english}
\setotherlanguage{arabic}
% Use a font that has Arabic glyphs; macOS ships with Geeza Pro.
\IfFontExistsTF{Geeza Pro}{\newfontfamily\arabicfont{Geeza Pro}}{%
  \IfFontExistsTF{Noto Sans Arabic}{\newfontfamily\arabicfont{Noto Sans Arabic}}{%
    \IfFontExistsTF{Amiri}{\newfontfamily\arabicfont{Amiri}}{}}}
\usepackage[margin=2.4cm, top=2.2cm, bottom=2.2cm]{geometry}
\usepackage{graphicx}
\usepackage{float}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{pdflscape}
\usepackage[table]{xcolor}
\usepackage{colortbl}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{hyperref}
\usepackage{mdframed}
\usepackage{enumitem}
\usepackage{parskip}
\usepackage{microtype}
\usepackage{array}
\usepackage{tabularx}
\usepackage{tikz}
\usepackage{listings}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{textcomp}

\renewcommand{\lstlistingname}{Code Snippet}
\renewcommand{\lstlistlistingname}{List of Code Snippets}

\usetikzlibrary{calc, positioning, arrows.meta, shapes.geometric}

% ── Plotly dark palette ───────────────────────────────────────────────
\definecolor{darkbg}{HTML}{0D1117}
\definecolor{darkpanel}{HTML}{161B22}
\definecolor{darkborder}{HTML}{30363D}
\definecolor{bodytext}{HTML}{C9D1D9}
\definecolor{dimtext}{HTML}{8B949E}
\definecolor{plotblue}{HTML}{58A6FF}
\definecolor{plotteal}{HTML}{2A9D8F}
\definecolor{plotorange}{HTML}{E86F51}
\definecolor{plotyellow}{HTML}{E9C46A}
\definecolor{plotpink}{HTML}{FF6B6B}
\definecolor{plotpurple}{HTML}{7B2D8E}
\definecolor{plotteal2}{HTML}{4ECDC4}

% ── Global page & text colour ─────────────────────────────────────────
\pagecolor{darkbg}
\color{bodytext}

% ── Section headings ──────────────────────────────────────────────────
\titleformat{\section}
  {\Large\bfseries\color{plotblue}}
  {\thesection.}{0.6em}{}[{\color{plotorange}\titlerule[1.5pt]}]

\titleformat{\subsection}
  {\large\bfseries\color{plotteal}}
  {\thesubsection.}{0.5em}{}

\titleformat{\subsubsection}
  {\normalsize\bfseries\color{plotblue}}
  {\thesubsubsection.}{0.5em}{}

\titlespacing*{\section}      {0pt}{18pt}{8pt}
\titlespacing*{\subsection}   {0pt}{14pt}{4pt}
\titlespacing*{\subsubsection}{0pt}{10pt}{3pt}

% ── Callout / insight box ─────────────────────────────────────────────
\newmdenv[
  backgroundcolor=darkpanel,
  linecolor=plotblue,
  linewidth=3pt,
  leftline=true, rightline=false, topline=false, bottomline=false,
  innerleftmargin=12pt, innerrightmargin=10pt,
  innertopmargin=10pt, innerbottommargin=10pt,
  skipabove=10pt, skipbelow=10pt,
  fontcolor=bodytext
]{callout}

% Variant for warnings / honesty notes
\newmdenv[
  backgroundcolor=darkpanel,
  linecolor=plotorange,
  linewidth=3pt,
  leftline=true, rightline=false, topline=false, bottomline=false,
  innerleftmargin=12pt, innerrightmargin=10pt,
  innertopmargin=10pt, innerbottommargin=10pt,
  skipabove=10pt, skipbelow=10pt,
  fontcolor=bodytext
]{warningbox}

% Variant for theorems / mathematical statements
\newmdenv[
  backgroundcolor=darkpanel,
  linecolor=plotteal,
  linewidth=3pt,
  leftline=true, rightline=false, topline=false, bottomline=false,
  innerleftmargin=12pt, innerrightmargin=10pt,
  innertopmargin=10pt, innerbottommargin=10pt,
  skipabove=10pt, skipbelow=10pt,
  fontcolor=bodytext
]{mathbox}

% ── Hyperlinks ────────────────────────────────────────────────────────
\hypersetup{
  colorlinks=true,
  linkcolor=plotblue,
  urlcolor=plotteal,
  pdftitle={Cairo Public Transport · Comprehensive Formal Report},
  pdfauthor={Mustafa, Moheeb, Mohammed, Farah}
}

% ── Header / Footer ───────────────────────────────────────────────────
\setlength{\headheight}{14pt}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\color{dimtext}Cairo Public Transport \textbullet\ Formal Report}
\fancyhead[R]{\small\color{dimtext}\leftmark}
\fancyfoot[C]{\small\color{dimtext}--- \thepage\ ---}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{{\color{darkborder}\hrule width\headwidth height 0.4pt}}

% ── Table rules ───────────────────────────────────────────────────────
\arrayrulecolor{darkborder}
\setlength{\heavyrulewidth}{1.2pt}
\setlength{\lightrulewidth}{0.4pt}

% ── Caption ───────────────────────────────────────────────────────────
\captionsetup{
  font={small, color=dimtext},
  labelfont={bf, color=plotblue},
  skip=5pt
}

% ── List bullets ──────────────────────────────────────────────────────
\setlist[itemize]{
  label={\color{plotorange}\textbullet},
  leftmargin=1.6cm, itemsep=2pt
}
\setlist[enumerate]{
  label={\color{plotorange}\arabic*.},
  leftmargin=1.6cm, itemsep=2pt
}

% ── Inline helpers ────────────────────────────────────────────────────
\newcommand{\kw}[1]{\textbf{\color{plotblue}#1}}
\newcommand{\stat}[1]{\textbf{\color{plotorange}#1}}
\newcommand{\good}[1]{\textbf{\color{plotteal}#1}}
\newcommand{\fn}[1]{\texttt{\color{plotteal}#1}}
\newcommand{\var}[1]{\texttt{\color{bodytext}#1}}
\newcommand{\strval}[1]{\texttt{\color{plotyellow}#1}}
\newcommand{\ar}[1]{\textbf{\color{plotyellow}\textarabic{#1}}}

% Figure include macro: \fig{path}{width fraction}{caption}{label}
\newcommand{\figitem}[4]{%
  \begin{figure}[H]
    \centering
    \includegraphics[width=#2\linewidth]{figures/#1}
    \caption{#3}
    \label{#4}
  \end{figure}%
}

% Side-by-side helper (two figures)
\newcommand{\figpair}[6]{%
  \begin{figure}[H]
    \centering
    \begin{subfigure}{0.49\linewidth}
      \centering\includegraphics[width=\linewidth]{figures/#1}
      \caption{#2}\label{#3}
    \end{subfigure}\hfill
    \begin{subfigure}{0.49\linewidth}
      \centering\includegraphics[width=\linewidth]{figures/#4}
      \caption{#5}\label{#6}
    \end{subfigure}
  \end{figure}%
}

% ═════════════════════════════════════════════════════════════════════
%  TERMINAL-STYLE CODE LISTING  (Plotly dark theme)
% ═════════════════════════════════════════════════════════════════════
\lstdefinestyle{plotlyterminal}{
  backgroundcolor  = \color{darkpanel},
  basicstyle       = \ttfamily\small\color{bodytext},
  keywordstyle     = \color{plotblue}\bfseries,
  keywordstyle     = [2]\color{plotteal},
  stringstyle      = \color{plotyellow},
  commentstyle     = \color{dimtext}\itshape,
  numberstyle      = \tiny\color{dimtext},
  frame            = single,
  rulecolor        = \color{darkborder},
  framesep         = 6pt,
  framerule        = 0.8pt,
  numbers          = left,
  numbersep        = 10pt,
  breaklines       = true,
  breakatwhitespace= true,
  tabsize          = 4,
  showstringspaces = false,
  language         = Python,
  morekeywords     = {True, False, None, self, cls, as, with, yield,
                      print, len, range, zip, enumerate, map, filter,
                      isinstance, type, abs, round, min, max, sum,
                      sorted, list, dict, set, tuple, str, int, float,
                      bool, pd, np, gpd, px, go, folium, scipy, sk},
  morekeywords     = [2]{DataFrame, GeoDataFrame, Series, Figure,
                         KNNImputer, NearestNeighbors, KMeans,
                         SentenceTransformer, kruskal, mannwhitneyu,
                         spearmanr, Moran, KNN, read_file, read_csv,
                         to_csv, merge, sjoin, groupby, rename, dropna,
                         fillna, copy, apply, agg, describe,
                         value_counts, reset_index, to_crs, buffer,
                         centroid, within, intersects, unary_union,
                         to_json, fit_transform, fit_predict, encode,
                         show, update_layout, add_trace, write_html,
                         write_image},
  captionpos       = t,
  abovecaptionskip = 4pt,
  belowcaptionskip = 4pt,
  aboveskip        = 8pt,
  belowskip        = 8pt,
}
\lstset{style=plotlyterminal}

% ═════════════════════════════════════════════════════════════════════
%  DECORATIVE TIKZ CHARTS (page-edge accents on cover pages)
% ═════════════════════════════════════════════════════════════════════
\newcommand{\decorbartopl}{%
  \begin{tikzpicture}[remember picture, overlay]
    \coordinate (TL) at (current page.north west);
    \foreach \y/\w/\col in {
      0.0/4.5/plotblue, 0.6/5.8/plotteal, 1.2/3.1/plotorange,
      1.8/4.8/plotblue, 2.4/2.5/dimtext, 3.0/3.9/plotteal,
      3.6/5.5/plotorange, 4.2/2.2/plotblue, 4.8/4.0/plotteal,
      5.4/3.4/dimtext, 6.0/1.8/plotorange}{
      \fill[\col] ([yshift=-\y cm]TL) rectangle ([xshift=\w cm, yshift=-\y cm - 0.4cm]TL);
    }
  \end{tikzpicture}%
}
\newcommand{\decorhistbr}{%
  \begin{tikzpicture}[remember picture, overlay]
    \coordinate (BR) at (current page.south east);
    \foreach \x/\h/\col in {
      0.0/5.2/plotorange, 0.6/2.5/plotblue, 1.2/4.6/dimtext,
      1.8/3.8/plotteal, 2.4/5.5/plotorange, 3.0/3.0/plotblue,
      3.6/4.2/plotteal, 4.2/5.0/dimtext, 4.8/2.8/plotblue,
      5.4/3.5/plotorange, 6.0/2.2/plotteal}{
      \fill[\col] ([xshift=-\x cm]BR) rectangle ([xshift=-\x cm - 0.4cm, yshift=\h cm]BR);
    }
  \end{tikzpicture}%
}

% Reusable section-cover page
\newcommand{\makesectioncover}[1]{%
  \clearpage
  \thispagestyle{empty}
  \decorbartopl
  \decorhistbr
  \begin{tikzpicture}[remember picture, overlay]
    \draw[plotblue, line width=1pt]
      ($(current page.north west)+(0,0)$) --
      ($(current page.north west)+(4cm,0)$);
    \draw[plotorange, line width=1pt]
      ($(current page.north west)+(4cm,0)$) --
      ($(current page.north east)+(0,0)$);
    \draw[plotblue, line width=1pt]
      ($(current page.south west)+(0,0)$) --
      ($(current page.south east)+(0,0)$);
  \end{tikzpicture}
  \vspace*{0pt}
  \begin{center}
    \vspace*{6.2cm}
    {\color{darkborder}\rule{0.2\linewidth}{1pt}}%
    {\color{plotorange}\rule{0.6\linewidth}{1pt}}%
    {\color{darkborder}\rule{0.2\linewidth}{1pt}}\\[0.7cm]
    {\fontsize{30}{38}\selectfont\bfseries\color{plotblue}#1\par}
    \vspace{0.5cm}
    {\color{plotorange}\rule{\linewidth}{0.8pt}}\\[0.4cm]
    {\large\color{dimtext}Cairo Public Transport · Formal Report\par}
    \vspace*{\fill}
    {\small\color{dimtext}25CSAI01I \quad\textbullet\quad
     Mustafa \quad Moheeb \quad Mohammed \quad Farah}
  \end{center}
  \clearpage
}
